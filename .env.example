# Copy to .env and fill in. The RAG synthesizer + QAEvalChain need an Anthropic API key.
ANTHROPIC_API_KEY=sk-ant-...

# --- Optional ---------------------------------------------------------------
# USE_LLM=0          # template/extractive only — runs the full app with no API key
# USE_DOC_RAG=0      # disable the qualitative PDF retriever (data-stats RAG only)
